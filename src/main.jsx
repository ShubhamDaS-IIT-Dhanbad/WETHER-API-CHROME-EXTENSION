import React from 'react';
import ReactDOM from 'react-dom';
import App from './App.jsx'; // Check the path and file extension

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
